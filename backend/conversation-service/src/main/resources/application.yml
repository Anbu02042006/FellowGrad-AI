server:
  port: 8083

spring:
  application:
    name: conversation-service
  data:
    mongodb:
      uri: mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/fellowgrad_conversations

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
